# Styles

This folder contains style definition files (`.json`) used by Criterion for text formatting and document styling.

Style files can be imported and exported through the application's style management interface.
