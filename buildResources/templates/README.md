# Templates

This folder contains document template files (`.json`) used by Criterion for creating new documents with predefined layouts, styles, and settings.

Template files can be imported and exported through the application's template management interface.
