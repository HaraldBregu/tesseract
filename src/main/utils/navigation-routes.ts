/**
 * Routes type definition for the whole application. Can be used in
 * main and renderer processes.
 */
export const NavigationRoutes = {
  root: '/',
  file: '/file',
  toolbar: '/browser-tab-bar'
}
